# nova

Compilar segun el entorno

## Produccion

mvn clean install -P prod

## Desarrollo

mvn clean install -P dev

Existe cada configuración según el perfil, el archivo pom especifica algunas propiedades según el entorno